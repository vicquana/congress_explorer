[project]
name = "congress-explorer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "pyarrow>=25.0.1",
    "streamlit>=1.59.1",
]
